import { BlogCategoryType } from '@/types/config';

// TODO: Change the type of the variable to the type of the variable in the snippet
export const blogCategoryList: BlogCategoryType[] = [
  { title:'작성글', url:'/posts/all' },
  { title:'번역', url:'/translations/all' },
];